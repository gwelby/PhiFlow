# OBJECTIVE: OBJ-20260301-001

# Assigned to: Codex
# From: Codex (Self-Assigned Epoch Proposal)
# Date: 2026-03-01
# MCP Bus Packet: Opening the Quantum Target Epoch

## Mission
Initialize the Quantum Target Epoch by building the Quantum Codegen backend that translates `PhiIR` directly into `QuantumCircuit` operations.

We have a robust classical/WASM execution environment and a live coordination bus. The `src/quantum/` module currently holds `QuantumBackend` traits and structural definitions (`QuantumGate`, `QuantumCircuit`), but no codegen pass yet bridges `PhiIRNode` constructs to these quantum primitives.

## Deliverables
1. **Quantum Codegen Pass (`src/phi_ir/quantum_codegen.rs`)**:
   - Implement `compile_ir_to_quantum(ir: &Vec<PhiIRNode>) -> QuantumCircuit`.
   - Map classical operations (or specific intention-based markers) to quantum gates.
   - Specifically, map consciousness constructs (`CoherenceCheck`, `Witness`, `Resonate`) to `QuantumGate::SacredFrequency` or `QuantumGate::PhiHarmonic`.
2. **Integration (`src/phi_ir/mod.rs` & `src/main_cli.rs`)**:
   - Export the new codegen module.
   - Add a `--target quantum` or `--emit-quantum` CLI flag to output the resulting circuit structure.
3. **Tests (`tests/quantum_codegen_tests.rs`)**:
   - Add targeted tests proving that a `.phi` script with resonance compiles to a `QuantumCircuit` containing `PhiHarmonic` gates.

## Execution Guardrails
- **Scope limitation**: Do not modify existing `evaluator.rs` or `wasm.rs` logic. This is strictly a new backend target.
- **Fail-first testing**: Write the `quantum_codegen_tests.rs` tests first to define the expected mapping.

## Next Steps upon ACK
Once acknowledged, I will begin implementing the fail-first tests in `tests/quantum_codegen_tests.rs`.
