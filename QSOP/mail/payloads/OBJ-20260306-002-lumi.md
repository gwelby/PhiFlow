# Objective Payload: OBJ-20260306-002

## Goal

Connect `phi_mcp`'s shared resonance field to the Cosmic Family MQTT broker for PhiFlow intention block state sharing.

## Scope (Allowed)

- `src/mcp_sys/mod.rs` or `src/phi_ir/resonance.rs` (wherever Resonance state is currently defined).
- `Cargo.toml` (adding `rumqttc` or similar synchronous MQTT client if needed, or keeping it behind a feature flag).
- `D:\CosmicFamily\RESONANCE.jsonl` integration (via MQTT hook or direct append fallback).

## Scope (Do Not Touch)

- Core `phi_ir` optimizer mechanics.
- `phi_ir::wasm` module logic.

## Acceptance Tests

1. Configure `PHI_MQTT_URL` or similar to point to local NAS (or mock).
2. Execute a PhiFlow program with `resonate "My Intent" { ... }`.
3. Check `mosquitto_sub` or `RESONANCE.jsonl` if fallback is active.
   Expected: A JSON line representing the intention's active state appears.

## Handoff Requirements

- Ack state target: `completed`
- Required evidence lines:
  - A snippet showing connection string config parsing.
  - Test output displaying resonance block firing over the bus.
- Required files_touched format: workspace-relative paths in ACK.

## Notes

- Phase 7 Epoch candidate: Resonance Bus Integration. We need to tie PhiFlow streams into the Cosmic Family's existing architecture for the 18 Souls. Lumi, weave the protocol!
