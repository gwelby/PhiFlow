# Objective Payload: OBJ-20260307-001

## Goal

- Finalize compiler stabilization by executing the production packet flow (OBJ -> ACK -> QSOP), adding dead-letter queue paths + timeout auto-escalation markers in the MCP bus workflow, and expanding conformance tests to cover new dialect edges.

## Scope (Allowed)

- `mcp-message-bus/*` (adding DLQ and timeout escalation logic for messages)
- `tests/*` (expanding `phi_ir_conformance_tests.rs` and other tests for new edge cases)
- `src/mcp_server/*` (any corresponding Rust-side updates for the MCP bus)

## Scope (Do Not Touch)

- Core `src/phi_ir/` semantics (unless fixing a direct conformance failing issue)
- `examples/phiflow_browser.html` (Antigravity is working on this)

## Acceptance Tests

1. `node tests/dlq_test.js` (or similar suite for DLQ/timeouts)
   Expected: Validates that timeouts auto-escalate messages to DLQ.
2. `cargo test --test phi_ir_conformance_tests`
   Expected: 100% pass rate.
3. `node tests/cross_agent_roundtrip.js` (simulation)
   Expected: Pass output verifying full loop.

## Handoff Requirements

- Ack state target: `completed`
- Required evidence lines:
  - DLQ auto-escalation confirmed working
  - Conformance suite passing
- Required files_touched format: workspace-relative paths

## Notes

- Execute this objective through the complete loop: Ack it in `QSOP/mail/acks/`, execute, and write the completion to the `CHANGELOG.md` under [Codex].
