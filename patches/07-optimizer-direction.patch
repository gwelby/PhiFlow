--- a/src/phi_ir/optimizer.rs
+++ b/src/phi_ir/optimizer.rs
@@ -280,7 +280,7 @@ impl Optimizer {
             // Resonate with a value is a side effect (broadcasts to resonance field)
             // Resonate without a value is a no-op (self-observation only)
             PhiIRNode::Resonate {
                 value: Some(_),
+                direction: _,
                 ..
             } => {
                 // Keep it - it's a side effect
@@ -285,7 +285,7 @@ impl Optimizer {
             PhiIRNode::Resonate {
                 value: None,
+                direction: _,
             } => {}
 
             // Control flow
