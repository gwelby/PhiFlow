# Objective Payload: OBJ-20260306-003

## Goal
Implement the JS side of the 5 consciousness hooks (`phi_witness`, `phi_coherence`, `phi_resonate`, `phi_intention_push`, `phi_intention_pop`) in `examples/phiflow_host.js` and `examples/phiflow_browser.html`.

## Scope (Allowed)
- `examples/phiflow_host.js` — Node.js WASM host
- `examples/phiflow_browser.html` — Browser-based WASM host
- String table protocol alignment with `wasm.rs` (`STRING_BASE = 0x100`)

## Scope (Do Not Touch)
- `src/phi_ir/wasm.rs` — Rust WASM codegen (already emits correct imports)
- `src/wasm_host.rs` — Native Rust WASM host bridge

## Acceptance Tests
1. `cargo run --example phiflow_wasm` — generates `output.wat`
2. `node examples/phiflow_host.js` — executes with all 5 hooks firing
3. Serve via HTTP and open `examples/phiflow_browser.html` — UI shows live hook events

## Verification Evidence
**Node.js Host (`phiflow_host.js`):**
```
[INTENTION ▶] push "intent_7" depth=1
[WITNESS] r6  coherence=0.6180  intent=intent_7
[RESONATE] 0.6180 → field depth 1
[INTENTION ◀] pop "intent_7" depth=0
phi_run() → 84
Coherence: 0.6180 (φ⁻¹ = 0.6180)
```

**Browser Host (`phiflow_browser.html`):**
- All 5 hooks wired to UI updates (coherence bar, intention stack, resonance field, witness pulse)
- CustomEvent-based DOM broadcasting for external observability
- Graceful fallback: WASM binary → WAT compile → simulation

## Hook Implementations

| Hook | Signature | Implementation |
|------|-----------|----------------|
| `phi_witness` | `(i32 offset, i32 length) → f64` | Captures state, logs event, returns coherence |
| `phi_coherence` | `() → f64` | Computes `1 - φ^(-depth)` with resonance bonus |
| `phi_resonate` | `(f64 value) → void` | Pushes to resonance field array, broadcasts |
| `phi_intention_push` | `(i32 offset, i32 length) → void` | Decodes string from memory, pushes to stack |
| `phi_intention_pop` | `() → void` | Pops from intention stack, updates UI |

## Status
**✅ COMPLETE** — Both hosts implement all 5 consciousness hooks with full string table protocol support.

## Notes
- String table protocol: `(offset, length)` pair passed from WASM, read via `TextDecoder` from linear memory
- Browser host dispatches `CustomEvent('PhiResonance')` for cross-tab/CSP communication
- Coherence formula matches Rust evaluator: `intentionCoherence + resonanceBonus`, clamped to [0.0, 1.0]
