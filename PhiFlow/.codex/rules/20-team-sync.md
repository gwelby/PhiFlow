# Team Sync Rules

- Use objective IDs for multi-agent work to prevent overlap.
- Claim ownership by file/subsystem before large edits.
- Include verification evidence in handoffs (command + key output).
- Prefer protocol over memory: write decisions to QSOP artifacts.
- Escalate blockers immediately instead of silently stalling.
