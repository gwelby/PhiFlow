--- a/src/phi_ir/quantum_codegen.rs
+++ b/src/phi_ir/quantum_codegen.rs
@@ -18,7 +18,7 @@ pub fn generate_quantum_circuit(ir: &PhiIRProgram) -> Result<QuantumCircuit, st
             match &inst.node {
                 PhiIRNode::Resonate {
                     value: _,
                     frequency_relationship,
+                    direction: _,
                 } => {
                     // In tests, "resonate 0.618" could pass the ratio here.
                     // We map this into a PhiHarmonic gate on qubit 0.
