--- a/src/interpreter/mod.rs
+++ b/src/interpreter/mod.rs
@@ -698,7 +698,7 @@ impl PhiInterpreter {
                 Ok(result)
             }
 
-            PhiExpression::Resonate { expression } => {
+            PhiExpression::Resonate { expression, direction: _ } => {
                 let current_intention = self
                     .intention_stack
                     .last()
